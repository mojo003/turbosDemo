<form method="post" action="{{ path('form_delete_demande_service', {'idDemandeService': DemandeService.idDemandeService}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ DemandeService.idDemandeService) }}">
    <button class="btn">Delete</button>
</form>
