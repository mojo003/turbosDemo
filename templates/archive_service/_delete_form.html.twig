<form method="post" action="{{ path('archive_service_delete', {'id': archive_service.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ archive_service.id) }}">
    {#<button class="btn">Delete</button> #}
</form>
