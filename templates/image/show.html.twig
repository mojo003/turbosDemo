{% extends 'base.html.twig' %}

{% block title %}Image{% endblock %}

{% block body %}
    <h1>Image</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdImage</th>
                <td>{{ image.idImage }}</td>
            </tr>
            <tr>
                <th>Caption</th>
                <td>{{ image.caption }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ image.description }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td>{{ image.img }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('image_index') }}">Retour à la liste</a>

    <a href="{{ path('image_edit', {'idImage': image.idImage}) }}">Editer</a>

    {{ include('image/_delete_form.html.twig') }}
{% endblock %}
