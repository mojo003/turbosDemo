<form method="post" action="{{ path('demande_de_service_delete', {'idDemandeService': demande_de_service.idDemandeService}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demande_de_service.idDemandeService) }}">
    <button class="btn">Delete</button>
</form>
